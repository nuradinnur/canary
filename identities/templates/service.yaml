apiVersion: v1
kind: Service
metadata:
  name: {{ include "identities.fullname" . }}
  labels:
    {{- include "identities.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "identities.selectorLabels" . | nindent 4 }}
