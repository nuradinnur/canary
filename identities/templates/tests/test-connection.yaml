apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "identities.fullname" . }}-test-connection"
  labels:
    {{- include "identities.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "identities.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
