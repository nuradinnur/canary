spring:
  cloud:
    environment:
      enabled: false
  flyway:
    baseline-on-migrate: true
    fail-on-missing-locations: true
    locations: classpath:db/migration/h2
    url: jdbc:h2:mem:///
  r2dbc:
    name: shared
    password: changeit
    username: dbuser
    url: r2dbc:h2:mem:///
