apiVersion: v1
kind: Service
metadata:
  name: {{ include "service-starter.fullname" . }}
  labels:
    {{- include "service-starter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "service-starter.selectorLabels" . | nindent 4 }}
